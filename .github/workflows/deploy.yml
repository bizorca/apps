name: Deploy to apps.bizorca.com

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  SSH_HOST: gcam1203.siteground.biz
  SSH_USER: u2361-smkk6swqmgxj
  SSH_PORT: "18765"
  REMOTE_PUBLIC: /home/customer/www/apps.bizorca.com/public_html

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -p "$SSH_PORT" "$SSH_HOST" >> ~/.ssh/known_hosts 2>/dev/null

      - name: Deploy via rsync
        run: |
          rsync -avz --delete \
            --exclude='.git' \
            --exclude='.github' \
            --exclude='.gitignore' \
            --exclude='deploy.sh' \
            --exclude='*.md' \
            --exclude='*.txt' \
            --exclude='.DS_Store' \
            -e "ssh -i ~/.ssh/deploy_key -p $SSH_PORT" \
            ./ "$SSH_USER@$SSH_HOST:$REMOTE_PUBLIC/"

      - name: Flush SiteGround dynamic cache
        run: |
          # `domain list` returns a top-level JSON array of domain objects.
          ssh -i ~/.ssh/deploy_key -p "$SSH_PORT" "$SSH_USER@$SSH_HOST" '
            id=$(site-tools-client -j domain list | python3 -c "import sys,json; print(next(d[\"id\"] for d in json.load(sys.stdin) if d.get(\"name\")==\"apps.bizorca.com\"))")
            test -n "$id"
            site-tools-client -j domain update id="$id" flush_cache=1
          '

      - name: Verify
        run: |
          code=$(curl -s -o /dev/null -w "%{http_code}" "https://apps.bizorca.com/?cb=${GITHUB_SHA}")
          echo "apps.bizorca.com returned $code"
          test "$code" = "200"
